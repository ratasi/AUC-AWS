{% extends '::base.html.twig' %}
{% block body %}
<div id="fh5co-tours">
        <div class="col-md-8 col-md-offset-2 text-center heading-section animate-box">
                <h3>Fútbol Convencional</h3>
            </div>
            <div class="container">
              {% for equipo in equipos %}
              <div class="col-md-4 col-sm-6 fh5co-tours animate-box" data-animate-effect="fadeIn" >
                  <div href="#"><img src="{{equipo.imagen}}" class="img-responsive-news">
                      <div class="desc">
                          <h3>{{ equipo.nombre }}</h3>
                          <a class="btn btn-primary btn-outline" href="futbol/equipo/{{equipo.id}}">Leer más<i class="icon-arrow-right22"></i></a>
                      </div>
                  </div>
              </div>
              {% endfor %}
                    </div>

{% endblock %}
